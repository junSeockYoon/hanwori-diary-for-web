<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>사용자 상세 - 한우리 다이어리</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f3f4f6;
            min-height: 100vh;
            padding: 24px;
        }

        .container {
            max-width: 960px;
            margin: 0 auto;
        }

        .card {
            background: #ffffff;
            border-radius: 16px;
            box-shadow: 0 12px 30px rgba(15, 23, 42, 0.18);
            overflow: hidden;
        }

        .card-header {
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 50%, #ec4899 100%);
            color: #ffffff;
            padding: 28px 32px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .card-header-title {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .card-header-title h1 {
            font-size: 24px;
            letter-spacing: -0.02em;
        }

        .card-header-title p {
            font-size: 14px;
            opacity: 0.9;
        }

        /* 코드 배지는 더 이상 사용하지 않으므로 스타일 제거 가능하지만, 혹시 몰라 보존 */

        .card-body {
            padding: 28px 32px 24px;
        }

        .section-title {
            font-size: 16px;
            font-weight: 700;
            color: #111827;
            margin-bottom: 16px;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 20px;
        }

        .field {
            padding: 14px 16px;
            border-radius: 12px;
            background: #f9fafb;
            border: 1px solid #e5e7eb;
        }

        .field-label {
            font-size: 12px;
            color: #6b7280;
            margin-bottom: 6px;
        }

        .field-value {
            font-size: 15px;
            color: #111827;
            font-weight: 600;
        }

        .field-value.mono {
            font-family: 'SF Mono', Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace;
            letter-spacing: 0.02em;
        }

        .user-type-pill {
            display: inline-flex;
            align-items: center;
            padding: 4px 10px;
            border-radius: 999px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .user-type-pill.admin {
            background-color: #fee2e2;
            color: #b91c1c;
        }

        .user-type-pill.user {
            background-color: #dcfce7;
            color: #15803d;
        }

        .status-pill {
            display: inline-flex;
            align-items: center;
            padding: 4px 10px;
            border-radius: 999px;
            font-size: 12px;
            font-weight: 600;
        }

        .status-pill.active {
            background-color: #ecfdf3;
            color: #166534;
        }

        .status-pill.inactive {
            background-color: #fef2f2;
            color: #991b1b;
        }

        .card-footer {
            padding: 16px 32px 20px;
            border-top: 1px solid #e5e7eb;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
        }

        .footer-left {
            font-size: 12px;
            color: #6b7280;
        }

        .footer-actions {
            display: flex;
            gap: 10px;
        }

        .btn {
            padding: 9px 18px;
            border-radius: 999px;
            border: none;
            cursor: pointer;
            font-size: 13px;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            text-decoration: none;
        }

        .btn-secondary {
            background-color: #f3f4f6;
            color: #111827;
        }

        .btn-secondary:hover {
            background-color: #e5e7eb;
        }

        .btn-primary {
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            color: #ffffff;
            box-shadow: 0 8px 18px rgba(79, 70, 229, 0.35);
        }

        .btn-primary:hover {
            filter: brightness(1.03);
        }

        @media (max-width: 640px) {
            .card-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 12px;
            }

            .card-body {
                padding: 20px 18px 16px;
            }

            .card-footer {
                padding: 14px 18px 18px;
                flex-direction: column;
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <% const user = userDetail && userDetail[0] ? userDetail[0] : null; %>

        <div class="card">
            <div class="card-header">
                <div class="card-header-title">
                    <h1>사용자 상세 정보</h1>
                    <% if (user) { %>
                        <p><strong><%= user.userId %></strong> 님의 계정 정보를 확인하고 사용자 유형을 변경할 수 있습니다.</p>
                    <% } else { %>
                        <p>사용자 정보를 불러오지 못했습니다.</p>
                    <% } %>
                </div>
            </div>

            <div class="card-body">
                <% if (user) { %>
                    <h2 class="section-title">기본 정보</h2>
                    <div class="grid" style="margin-bottom: 20px;">
                        <div class="field">
                            <div class="field-label">사용자 ID (user_id)</div>
                            <div class="field-value"><%= user.userId %></div>
                        </div>
                        <div class="field">
                            <div class="field-label">사용자 유형 (user_type)</div>
                            <div class="field-value">
                                <label style="font-size: 13px; color:#374151; margin-right:10px;">역할 선택:</label>
                                <label style="margin-right: 10px; font-size: 13px;">
                                    <input type="radio" name="userType" value="user" <%= user.userType === 'user' ? 'checked' : '' %> />
                                    일반 사용자
                                </label>
                                <label style="font-size: 13px;">
                                    <input type="radio" name="userType" value="admin" <%= user.userType === 'admin' ? 'checked' : '' %> />
                                    관리자
                                </label>
                            </div>
                        </div>
                        <div class="field">
                            <div class="field-label">상태 (use_yn)</div>
                            <div class="field-value">
                                <span class="status-pill <%= user.useYn === 'Y' ? 'active' : 'inactive' %>">
                                    <%= user.useYn === 'Y' ? '활성' : '비활성' %>
                                </span>
                            </div>
                        </div>
                    </div>
                <% } else { %>
                    <p style="padding: 12px 0; color: #6b7280;">표시할 사용자 정보가 없습니다.</p>
                <% } %>
            </div>

            <div class="card-footer">
                <div class="footer-left">
                    <% if (user) { %>
                        사용자 유형을 변경한 뒤, 저장 버튼을 눌러주세요.
                    <% } else { %>
                        사용자 상세 정보 로딩 실패.
                    <% } %>
                </div>
                <div class="footer-actions">
                    <a href="/admin/userManagement" class="btn btn-secondary">← 사용자 목록으로</a>
                    <% if (user) { %>
                        <button class="btn btn-primary" type="button" onclick="updateUserType()">
                            ✏️ 사용자 유형 저장
                        </button>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
    <% if (user) { %>
    <script>
        async function updateUserType() {
            const selected = document.querySelector('input[name="userType"]:checked');
            if (!selected) {
                alert('변경할 사용자 유형을 선택해주세요.');
                return;
            }

            const userType = selected.value;
            const userCd = '<%= user.userCd %>';

            if (!confirm(`사용자 유형을 \"${userType === 'admin' ? '관리자' : '일반 사용자'}\"(으)로 변경하시겠습니까?`)) {
                return;
            }

            try {
                const response = await fetch(`/admin/api/userManagement/userType/${userCd}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ userType })
                });

                const result = await response.json();

                if (response.ok && result.success) {
                    alert('사용자 유형이 성공적으로 변경되었습니다.');
                    // 변경된 정보 반영을 위해 새로고침
                    location.reload();
                } else {
                    alert(result.message || '사용자 유형 변경 중 오류가 발생했습니다.');
                }
            } catch (error) {
                console.error('사용자 유형 변경 에러:', error);
                alert('서버 오류가 발생했습니다.');
            }
        }
    </script>
    <% } %>
</body>
</html>


