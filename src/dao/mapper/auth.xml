<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="auth">

    <select id="userType">
        SELECT 
            user_type ,
            user_cd
        FROM users 
        WHERE USER_ID = #{id} and PW = #{password}
    </select>

    <insert id="insertUser">
        INSERT INTO users (user_id, pw, user_type, use_yn)
        VALUES (#{id}, #{password}, 'user', 'Y')
    </insert>

    <select id="userTypeApi">
        SELECT 
            user_type 
        FROM users 
        WHERE use_yn = 'Y'
        and user_cd = #{userCd}
    </select>

    <select id="userList">
        SELECT 
            user_cd,
            user_id,
            user_type,
            use_yn
        FROM users 
        WHERE use_yn = 'Y'
    </select>

    <select id="userDetail">
        SELECT 
            user_cd,
            user_id,
            user_type,
            use_yn
        FROM users 
        WHERE use_yn = 'Y'
        and user_cd = #{userCd}
        order by create_dt desc
    </select>

    <update id="updateUserType">
        UPDATE users 
        SET user_type = #{userType},
            modify_dt = NOW()
        WHERE user_cd = #{userCd}
    </update>
     
</mapper>