<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원가입 - 한우리 다이어리</title>
    <style>
        :root {
            --bg-start: #f0f4ff;
            --bg-end: #e8f6f3;
            --card-bg: #ffffff;
            --text-primary: #1f2937; /* gray-800 */
            --text-secondary: #6b7280; /* gray-500 */
            --primary: #4f46e5; /* indigo-600 */
            --primary-hover: #4338ca; /* indigo-700 */
            --border: #e5e7eb; /* gray-200 */
            --shadow: 0 10px 25px rgba(0, 0, 0, 0.647);
            --radius: 16px;
        }
        * { box-sizing: border-box; }
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen,
                Ubuntu, Cantarell, 'Helvetica Neue', Arial, 'Apple Color Emoji',
                'Segoe UI Emoji', 'Segoe UI Symbol';
            color: var(--text-primary);
            background: radial-gradient(1200px 800px at 10% 10%, #eef2ff 0%, rgba(238,242,255,0) 60%),
                        radial-gradient(1000px 800px at 90% 20%, #ecfeff 0%, rgba(236,254,255,0) 55%),
                        linear-gradient(135deg, var(--bg-start), var(--bg-end));
            min-height: 100vh;
        }

        .main-container {
            width: 100vw;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 24px;
        }

        .signup-container {
            width: 100%;
            max-width: 420px;
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            padding: 32px;
        }

        .signup-header {
            text-align: center;
            margin-bottom: 24px;
        }

        .brand {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 56px;
            height: 56px;
            border-radius: 50%;
            background: #eef2ff;
            color: #3730a3;
            font-weight: 800;
            font-size: 18px;
            margin-bottom: 12px;
        }

        .signup-title {
            margin: 0 0 6px;
            font-size: 24px;
            font-weight: 800;
            letter-spacing: -0.02em;
        }

        .signup-subtitle {
            margin: 0;
            font-size: 14px;
            color: var(--text-secondary);
        }

        .input-group {
            margin-top: 16px;
        }

        .input-label {
            display: block;
            font-size: 13px;
            font-weight: 600;
            color: #374151; /* gray-700 */
            margin-bottom: 8px;
        }

        .input-control {
            width: 100%;
            padding: 12px 14px;
            border: 1px solid var(--border);
            border-radius: 10px;
            background: #fff;
            font-size: 14px;
            transition: border-color .15s ease, box-shadow .15s ease;
        }

        .input-control:focus {
            outline: none;
            border-color: #c7d2fe; /* indigo-200 */
            box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.15);
        }

        .helper {
            margin-top: 8px;
            font-size: 12px;
            color: var(--text-secondary);
        }

        .signup-button {
            width: 100%;
            margin-top: 16px;
            height: 44px;
            background-color: var(--primary);
            color: #fff;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 700;
            font-size: 15px;
            letter-spacing: 0.01em;
            transition: transform .05s ease, background-color .15s ease, box-shadow .15s ease;
            box-shadow: 0 8px 18px rgba(79, 70, 229, 0.25);
        }

        .signup-button:hover { background-color: var(--primary-hover); }
        .signup-button:active { transform: translateY(1px); }

        .login-link {
            width: 100%;
            margin-top: 12px;
            height: 44px;
            background-color: #fff;
            color: var(--primary);
            border: 2px solid var(--primary);
            border-radius: 10px;
            cursor: pointer;
            font-weight: 700;
            font-size: 15px;
            letter-spacing: 0.01em;
            transition: transform .05s ease, background-color .15s ease, color .15s ease;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .login-link:hover {
            background-color: var(--primary);
            color: #fff;
        }

        .login-link:active {
            transform: translateY(1px);
        }

        .footer {
            margin-top: 18px;
            text-align: center;
            font-size: 12px;
            color: var(--text-secondary);
        }

        .error-message {
            background-color: #fee2e2;
            color: #dc2626;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 16px;
            font-size: 14px;
            text-align: center;
            border: 1px solid #fecaca;
        }

        .success-message {
            background-color: #d1fae5;
            color: #065f46;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 16px;
            font-size: 14px;
            text-align: center;
            border: 1px solid #a7f3d0;
        }

        @media (max-width: 480px) {
            .signup-container { padding: 24px; }
            .signup-title { font-size: 22px; }
        }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="signup-container">
            <div class="signup-header">
                <div class="brand">HW</div>
                <h1 class="signup-title">회원가입</h1>
                <p class="signup-subtitle">새로운 계정을 만들어보세요</p>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>

            <% if (typeof success !== 'undefined' && success) { %>
                <div class="success-message">
                    <%= success %>
                </div>
            <% } %>

            <form method="POST" action="/auth/api/signup" id="signupForm">
                <div class="input-group">
                    <label for="id" class="input-label">아이디</label>
                    <input type="text" id="id" name="id" class="input-control" placeholder="아이디를 입력하세요" required>
                    <div class="helper">영문, 숫자 조합 4-20자</div>
                </div>

                <div class="input-group">
                    <label for="password" class="input-label">비밀번호</label>
                    <input type="password" id="password" name="password" class="input-control" placeholder="비밀번호를 입력하세요" required>
                    <div class="helper">최소 8자 이상</div>
                </div>

                <div class="input-group">
                    <label for="passwordConfirm" class="input-label">비밀번호 확인</label>
                    <input type="password" id="passwordConfirm" name="passwordConfirm" class="input-control" placeholder="비밀번호를 다시 입력하세요" required>
                </div>

                <button type="submit" class="signup-button">회원가입</button>
            </form>

            <a href="/auth/login" class="login-link">이미 계정이 있으신가요? 로그인</a>

            <div class="footer">© <%= new Date().getFullYear() %> Hanwoori Diary</div>
        </div>
    </div>
    <script>
        // 비밀번호 확인 검증
        document.getElementById('signupForm').addEventListener('submit', function(e) {
            const password = document.getElementById('password').value;
            const passwordConfirm = document.getElementById('passwordConfirm').value;

            if (password !== passwordConfirm) {
                e.preventDefault();
                alert('비밀번호가 일치하지 않습니다.');
                return false;
            }

            if (password.length < 8) {
                e.preventDefault();
                alert('비밀번호는 최소 8자 이상이어야 합니다.');
                return false;
            }
        });
    </script>
</body>
</html>

